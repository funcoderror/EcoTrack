# Server Configuration
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=your_neon_database_url_here

# JWT Configuration
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_32_characters

# Client URL (Frontend URL)
CLIENT_URL=http://localhost:5173

# Production Settings
# For production deployment on Vercel:
# 1. Set NODE_ENV=production
# 2. Set CLIENT_URL to your frontend domain (e.g., https://your-app.vercel.app)
# 3. Generate a strong JWT_SECRET_KEY (use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# 4. Ensure DATABASE_URL is set with proper SSL settings
